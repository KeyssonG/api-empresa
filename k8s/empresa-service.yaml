apiVersion: v1
kind: Service
metadata:
  name: empresa-service
spec:
  selector:
    app: empresa-deployment
  ports:
    - protocol: TCP
      port: 8085
      targetPort: 8085
  type: LoadBalancer  # Ou NodePort
